import TsResizeObserverComponent from './resize-observer.component.js';

export class TsResizeObserver extends TsResizeObserverComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsResizeObserverComponent.
}
