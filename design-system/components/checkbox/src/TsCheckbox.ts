import TsCheckboxComponent from './checkbox.component.js';

export class TsCheckbox extends TsCheckboxComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsCheckboxComponent.
}
