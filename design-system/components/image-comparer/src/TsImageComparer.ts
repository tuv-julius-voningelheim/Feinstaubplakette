import TsImageComparerComponent from './image-comparer.component.js';

export class TsImageComparer extends TsImageComparerComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsImageComparerComponent.
}
