import TsButtonGroupComponent from './button-group.component.js';

export class TsButtonGroup extends TsButtonGroupComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsButtonGroupComponent.
}
