import TsCardComponent from './card.component.js';

export class TsCard extends TsCardComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsCardComponent.
}
