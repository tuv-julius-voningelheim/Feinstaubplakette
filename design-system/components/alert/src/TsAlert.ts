import TsAlertComponent from './alert.component.js';

export class TsAlert extends TsAlertComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsAlertComponent.
}
