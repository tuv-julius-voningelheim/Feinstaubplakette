import TsPopupComponent from './popup.component.js';

export class TsPopup extends TsPopupComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsPopupComponent.
}
