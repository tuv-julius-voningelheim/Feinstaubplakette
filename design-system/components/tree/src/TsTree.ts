import TsTreeComponent from './tree.component.js';

export class TsTree extends TsTreeComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsTreeComponent.
}
