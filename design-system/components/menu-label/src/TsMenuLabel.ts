import TsMenuLabelComponent from './menu-label.component.js';

export class TsMenuLabel extends TsMenuLabelComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsMenuLabelComponent.
}
