import TsRadioGroupComponent from './radio-group.component.js';

export class TsRadioGroup extends TsRadioGroupComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsRadioGroupComponent.
}
