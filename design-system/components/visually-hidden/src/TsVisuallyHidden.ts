import TsVisuallyHiddenComponent from './visually-hidden.component.js';

export class TsVisuallyHidden extends TsVisuallyHiddenComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsVisuallyHiddenComponent.
}
