import TsRelativeTimeComponent from './relative-time.component.js';

export class TsRelativeTime extends TsRelativeTimeComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsRelativeTimeComponent.
}
