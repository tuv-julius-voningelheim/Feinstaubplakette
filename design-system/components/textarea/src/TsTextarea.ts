import TsTextareaComponent from './textarea.component.js';

export class TsTextarea extends TsTextareaComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsTextareaComponent.
}
