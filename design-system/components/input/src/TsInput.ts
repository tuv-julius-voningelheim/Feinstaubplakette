import TsInputComponent from './input.component.js';

export class TsInput extends TsInputComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsInputComponent.
}
