import TsDateRangeComponent from './date-range.component.js';

export class TsDateRange extends TsDateRangeComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsDateRangeComponent.
}
