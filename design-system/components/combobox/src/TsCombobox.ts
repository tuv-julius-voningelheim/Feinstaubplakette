import TsComboboxComponent from './combobox.component.js';

export class TsCombobox extends TsComboboxComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsComboboxComponent.
}
