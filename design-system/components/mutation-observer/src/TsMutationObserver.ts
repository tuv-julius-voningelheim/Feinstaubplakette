import TsMutationObserverComponent from './mutation-observer.component.js';

export class TsMutationObserver extends TsMutationObserverComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsMutationObserverComponent.
}
