import TsCopyButtonComponent from './copy-button.component.js';

export class TsCopyButton extends TsCopyButtonComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsCopyButtonComponent.
}
