import TsMenuItemComponent from './menu-item.component.js';

export class TsMenuItem extends TsMenuItemComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsMenuItemComponent.
}
