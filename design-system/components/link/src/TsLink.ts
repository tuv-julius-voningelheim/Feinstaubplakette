import TsLinkComponent from './link.component.js';

export class TsLink extends TsLinkComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsLinkComponent.
}
