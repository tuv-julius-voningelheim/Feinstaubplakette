import TsButtonComponent from './button.component.js';

export class TsButton extends TsButtonComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsButtonComponent.
}
