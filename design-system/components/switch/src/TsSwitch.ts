import TsSwitchComponent from './switch.component.js';

export class TsSwitch extends TsSwitchComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsSwitchComponent.
}
