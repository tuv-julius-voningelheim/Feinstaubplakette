import TsSkeletonComponent from './skeleton.component.js';

export class TsSkeleton extends TsSkeletonComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsSkeleton.
}
