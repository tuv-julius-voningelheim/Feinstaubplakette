import TsAvatarComponentGroup from './avatar-group.component.js';

export class TsAvatarGroup extends TsAvatarComponentGroup {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsAvatarComponentGroup.
}
