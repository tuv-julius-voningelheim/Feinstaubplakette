import TsRatingComponent from './rating.component.js';

export class TsRating extends TsRatingComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsRatingComponent.
}
