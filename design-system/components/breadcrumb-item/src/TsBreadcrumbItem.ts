import TsBreadcrumbItemComponent from './breadcrumb-item.component.js';

export class TsBreadcrumbItem extends TsBreadcrumbItemComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsBreadcrumbItemComponent.
}
