import TsQrCodeComponent from './qr-code.component.js';

export class TsQrCode extends TsQrCodeComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsQrCodeComponent.
}
