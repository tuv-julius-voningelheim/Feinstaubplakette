import TsToastComponent from './toast.component.js';

export class TsToast extends TsToastComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsToastComponent.
}
