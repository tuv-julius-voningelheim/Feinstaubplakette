import TsTabComponent from './tab.component.js';

export class TsTab extends TsTabComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsTabComponent.
}
