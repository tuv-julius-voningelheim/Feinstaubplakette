import TsFormatNumberComponent from './format-number.component.js';

export class TsFormatNumber extends TsFormatNumberComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsFormatNumberComponent.
}
