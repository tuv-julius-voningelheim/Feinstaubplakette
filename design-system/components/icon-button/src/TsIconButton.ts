import TsIconButtonComponent from './icon-button.component.js';

export class TsIconButton extends TsIconButtonComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsIconButtonComponent.
}
