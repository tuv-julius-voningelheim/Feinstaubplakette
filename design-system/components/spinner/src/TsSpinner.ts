import TsSpinnerComponent from './spinner.component.js';

export class TsSpinner extends TsSpinnerComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsSpinnerComponent.
}
