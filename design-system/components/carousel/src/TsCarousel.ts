import TsCarouselComponent from './carousel.component.js';

export class TsCarousel extends TsCarouselComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsCarouselComponent.
}
