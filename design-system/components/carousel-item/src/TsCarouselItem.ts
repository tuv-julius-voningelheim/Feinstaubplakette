import TsCarouselItemComponent from './carousel-item.component.js';

export class TsCarouselItem extends TsCarouselItemComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsCarouselItemComponent.
}
