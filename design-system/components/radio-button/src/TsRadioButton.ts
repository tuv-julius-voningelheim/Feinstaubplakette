import TsRadioButtonComponent from './radio-button.component.js';

export class TsRadioButton extends TsRadioButtonComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsRadioButtonComponent.
}
