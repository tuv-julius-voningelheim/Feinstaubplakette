import TsColorPickerComponent from './color-picker.component.js';

export class TsColorPicker extends TsColorPickerComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsColorPickerComponent.
}
