import TsAnimatedImageComponent from './animated-image.component.js';

export class TsAnimatedImage extends TsAnimatedImageComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsAnimatedImageComponent.
}
