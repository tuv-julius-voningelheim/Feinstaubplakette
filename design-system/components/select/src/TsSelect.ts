import TsSelectComponent from './select.component.js';

export class TsSelect extends TsSelectComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsSelectComponent.
}
