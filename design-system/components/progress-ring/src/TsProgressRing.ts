import TsProgressRingComponent from './progress-ring.component.js';

export class TsProgressRing extends TsProgressRingComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsProgressRingComponent.
}
