import TsIconComponent from './icon.component.js';

export class TsIcon extends TsIconComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsIconComponent.
}
