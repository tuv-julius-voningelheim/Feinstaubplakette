import TsTagComponent from './tag.component.js';

export class TsTag extends TsTagComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsTagComponent.
}
