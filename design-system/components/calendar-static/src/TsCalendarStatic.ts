import TsCalendarStaticComponent from './calendar-static.component.js';

export class TsCalendarStatic extends TsCalendarStaticComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsCalendarStaticComponent.
}
