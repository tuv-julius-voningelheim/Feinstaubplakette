import TsDrawerComponent from './drawer.component.js';

export class TsDrawer extends TsDrawerComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsDrawerComponent.
}
