import TsAccordionItemComponent from './accordion-item.component.js';

export class TsAccordionItem extends TsAccordionItemComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsAccordionItemComponent.
}
