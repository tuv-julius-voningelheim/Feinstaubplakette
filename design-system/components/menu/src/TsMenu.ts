import TsMenuComponent from './menu.component.js';

export class TsMenu extends TsMenuComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsMenuComponent.
}
