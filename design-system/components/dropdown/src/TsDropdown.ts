import TsDropdownComponent from './dropdown.component.js';

export class TsDropdown extends TsDropdownComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsDropdownComponent.
}
