import TsAccordionComponent from './accordion.component.js';

export class TsAccordion extends TsAccordionComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsAccordionComponent.
}
