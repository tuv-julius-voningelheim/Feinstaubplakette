import TsDropzoneComponent from './dropzone.component.js';

export class TsDropzone extends TsDropzoneComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsDropzoneComponent.
}
