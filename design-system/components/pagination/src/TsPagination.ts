import TsPaginationComponent from './pagination.component.js';

export class TsPagination extends TsPaginationComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsPaginationComponent.
}
