import TsDatePickerComponent from './date-picker.component.js';

export class TsDatePicker extends TsDatePickerComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsDatePickerComponent.
}
