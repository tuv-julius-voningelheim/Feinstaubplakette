import TsAvatarComponent from './avatar.component.js';

export class TsAvatar extends TsAvatarComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsAvatarComponent.
}
