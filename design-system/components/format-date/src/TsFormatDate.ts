import TsFormatDateComponent from './format-date.component.js';

export class TsFormatDate extends TsFormatDateComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsFormatDateComponent.
}
