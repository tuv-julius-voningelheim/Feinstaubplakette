import TsStepperComponent from './stepper.component.js';

export class TsStepper extends TsStepperComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsStepperComponent.
}
