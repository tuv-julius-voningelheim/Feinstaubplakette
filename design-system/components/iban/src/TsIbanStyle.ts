import { css } from 'lit';

export default css`
    :host {
        font-family: var(--ts-semantic-typography-font-family-default), system-ui, sans-serif;
    }

    .input {
        font-family: inherit;
    }
`;
