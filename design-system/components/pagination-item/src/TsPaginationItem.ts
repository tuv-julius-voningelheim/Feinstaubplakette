import TsPaginationItemComponent from './pagination-item.component.js';

export class TsPaginationItem extends TsPaginationItemComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsPaginationItemComponent.
}
