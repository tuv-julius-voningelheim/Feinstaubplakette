import TsTooltipComponent from './tooltip.component.js';

export class TsTooltip extends TsTooltipComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsTooltipComponent.
}
