import TsTabPanelComponent from './tab-panel.component.js';

export class TsTabPanel extends TsTabPanelComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsTabPanelComponent.
}
