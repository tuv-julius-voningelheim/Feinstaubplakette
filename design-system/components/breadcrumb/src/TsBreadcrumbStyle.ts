import { css } from 'lit';

export default css`
    .breadcrumb {
        display: flex;
        align-items: center;
        flex-wrap: wrap;
    }

    .breadcrumb__list {
        display: flex;
        list-style: none;
        padding: 0;
        margin: 0;
        gap: 0;
    }
`;
