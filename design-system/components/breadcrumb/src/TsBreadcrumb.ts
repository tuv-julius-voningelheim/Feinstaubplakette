import TsBreadcrumbComponent from './breadcrumb.component.js';

export class TsBreadcrumb extends TsBreadcrumbComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsBreadcrumbComponent.
}
