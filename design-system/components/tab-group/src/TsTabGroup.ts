import TsTabGroupComponent from './tab-group.component.js';

export class TsTabGroup extends TsTabGroupComponent {
    // This local component does not require any additional style overrides,
    // as all necessary styles are already managed within TsTabGroupComponent.
}
